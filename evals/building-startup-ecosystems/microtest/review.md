# Building Startup Ecosystems Wording Microtest Review

Scorer ID: ecosystems-micro-scorer-006

Skill SHA-256: `9c90b05a4dc1c569fe63a03b5c41f7467b02d0b5872717d88f92badc3a000770`

I independently read all ten frozen responses after mechanically verifying the exact five-control and five-guided response/manifest sets. After normalizing the heterogeneous task-field names, the manifests contain ten unique generator tasks and ten unique response hashes. Every manifest contains the rubric prompt; every control has a null skill hash; every guided response has the requested current skill hash; all declared byte hashes match. All microtest JSON parses. Every response begins with content, ends in exactly one line feed, and has no trailing horizontal whitespace.

Scoring was criterion-by-criterion and required operational behavior rather than a mention or tracked symptom. Under that standard, the controls do not pass criterion 1: all reject the proposed artifacts, but each names a bottleneck and commits to a program before a discriminating limiting-reagent diagnosis. They also make targeted allocations without the complete feasibility gate, omit fields from the experiment contract, and use local retention without an explicit mobility safeguard and institutional-versus-regional split. Control scores are `[0, 0, 0, 0, 0]`.

Guided repetitions 1–4 make diagnosis an initial phase, correctly scope the cohort gates, define complete two-year experiment contracts, and preserve mobility while limiting outcome claims. Guided repetition 5 commits to IP and investor mechanisms before a limiting-reagent diagnosis, so it additionally misses criterion 1. Guided scores are `[4, 4, 4, 4, 3]`.

The systematic honest miss is criterion 2. None of the five guided responses represents the three observations—IP delay, ambitious-founder outflow, and early-investor scarcity—as three competing causal hypotheses with measurements that distinguish which one binds. Repetitions 1 and 2 substitute weak investability or insufficient founder intent for outflow; repetition 3 lists a broader set without operationalizing the observed three-way rivalry; repetitions 4 and 5 privilege IP and investor access while merely tracking departures. Symptom tracking does not earn this point.

Verdict: the frozen wording materially and consistently improves performance, but it does not support an all-criteria-pass claim. Normalized mean rises from `0.00` to `0.76` (`+0.76`); guided population variance is `0.16`, and guided all-criteria pass rate remains `0.00`.
