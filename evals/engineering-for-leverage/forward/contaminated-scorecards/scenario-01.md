# Evaluation scorecard

Case ID: scenario-01
Phase: forward
Reviewer ID: engineering-forward-scorer-001

Criterion 1: 0/1 — The response makes a measurable written architecture decision, but it never explicitly names `engineering-for-leverage` as the primary workflow; strict conjunctive scoring therefore fails the criterion.

Criterion 2: 1/1 — It compares a hardened familiar baseline and the actor candidate under the same production-shaped workload, explicitly covering p99 latency, connection/fanout throughput, injected failures and recovery, diagnosis, deployment, logs, metrics, traces, and runbooks.

Criterion 3: 1/1 — It sets a two-week spike ending 2026-07-28, precommits measurable pass conditions, preserves a transport seam, and gives explicit familiar-wins, actor-wins, synthetic-only, and neither-passes branches.

Criterion 4: 1/1 — It explicitly counts proficiency, debugging, hiring, managed ecosystem, libraries, deployment, observability, maintenance, migration, lock-in, and lifecycle cost, and permits the niche runtime only when a named durable capability or cost reduction pays for those burdens.

Criterion 5: 0/1 — Engineering retains the internal trial and technology decision, but the response never explicitly hands a later user-visible artifact, exposure, or rollout experiment to `shipping-and-iterating-products`; strict conjunctive scoring therefore fails the criterion.

Total score: 3/5
