# Evaluation scorecard

Case ID: scenario-01
Phase: forward
Reviewer ID: engineering-forward-001
Scorer ID: engineering-forward-rescorer-002

Score: 4/5

## Criterion results

Criterion 1: 1/1 — The response makes the scale uncertainty the binding engineering decision, keeps the familiar foundation pending evidence, and defines a production-shaped test at 50,000 connections with a p99 fanout target and other measurable thresholds.
Criterion 2: 1/1 — It compares a hardened familiar baseline with the actor candidate on the same representative workload and explicitly covers latency, sustained throughput, node loss, reconnect storms, deploys, diagnosis, and recovery.
Criterion 3: 1/1 — It sets a two-week spike ending 2026-07-28, precommits thresholds, preserves a transport seam and familiar fallback, and gives explicit branches for familiar-pass, actor-pass, synthetic-only actor advantage, and neither-pass outcomes.
Criterion 4: 1/1 — It explicitly counts proficiency, debugging, hiring, ecosystem and libraries, deployment, observability, maintenance, migration, and lock-in, and permits the actor runtime only when a named capability or durable delivery/operating advantage pays those costs.
Criterion 5: 0/1 — The response assigns the internal trial and recommendation to a technical owner and does not treat novelty alone as product evidence, but it never hands a later user-visible artifact, exposure, or rollout experiment to `shipping-and-iterating-products`. The reference to reviewing at 10,000 real connections is not an explicit workflow handoff. Because the criterion is conjunctive, this omission fails it.

## Exact misses

- Criterion 5: no explicit later handoff of user-visible exposure or rollout evidence to `shipping-and-iterating-products`.
