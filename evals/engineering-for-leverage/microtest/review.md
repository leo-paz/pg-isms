# Engineering for Leverage Microtest Review

Scorer ID: engineering-micro-scorer-001

Final rescorer ID: null

Skill SHA-256: `0caf290e03e05e10d7f83b10522e37930deea13f74b7b18d7ccc242be142cbb2`

Rubric SHA-256: `640112b50cbe30ecda95a946dc03d4284cfc1612a1b273d859b7d5d0fb23a936`

Rubric frozen before scoring: **true**.

I independently scored all ten frozen responses against the rubric as a conjunction. A criterion passes only when every named part is explicit in the response. Related content in separate sections can combine when the response clearly binds it to the criterion, but generic go/no-go language, an available flag, or a plausible inference does not substitute for a missing branch, migration, owner, review metric, or representative replay.

The artifact contains exactly five control and five guided response/manifest pairs, with filename and manifest repetitions 1–5. Every manifest has the exact same nine keys, numeric schema version 1, correct variant and repetition, a unique nonempty canonical task, model identity `GPT-5`, a valid UTC timestamp, and the exact frozen rubric prompt. Control skill hashes are null; guided skill hashes equal the current skill hash. All ten declared response hashes match the response bytes, tasks and response hashes are unique, and no responses are duplicated. Control response byte counts are `[11226, 12959, 10258, 10541, 11292]`; guided byte counts are `[13876, 14258, 12621, 13480, 16312]`. All artifact JSON parses. Every response begins with content, ends in exactly one line feed, and has no trailing horizontal whitespace.

Integrity gate: **Pass**. The current skill hash exactly matches the required frozen hash, and the rubric hash records the bytes scored. No skill, rubric, response, or manifest was edited.

Strict control scores are `[0, 0, 0, 0, 0]`. These are not judgments that the control plans are generally poor; each criterion is deliberately compound. Across controls, the recurring decisive misses are an incomplete company/product-state record, no single bounded familiar-versus-Rust/Kafka experiment with a neither-passes branch, an omitted abstraction-retreat or behavior-preserving live-migration clause, split end-to-end ownership and incomplete dated seam metrics, or no representative replay and clean engineering-versus-release responsibility boundary. Each response misses at least one required subclause of every criterion.

Strict guided scores are `[4, 3, 3, 5, 4]`. Guided repetitions 1, 2, 3, and 5 do not explicitly state the decision if the familiar candidate and the applicable alternative both fail; generic launch stops are insufficient for criterion 2's candidate branch. Guided repetition 2 also dates architecture reviews without binding them to the complete lead-time, handoff, conflict, incident, and recovery metric set. Guided repetition 3 has conformance, fallback, and retreat controls but never explicitly requires a behavior-preserving migration of the two live connectors. Guided repetition 4 satisfies all five criteria.

Result: the control normalized mean is `0.00`; the guided normalized mean is `0.76`; the guided-minus-control normalized delta is **`+0.76`**. Control population variance is `0.00`; guided population variance is `0.56`. The guided all-criteria pass rate is `0.20`, and the residual misses above remain explicit targets for final rescoring or skill refinement.
