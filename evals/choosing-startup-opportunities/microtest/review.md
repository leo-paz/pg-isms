# Choosing Startup Opportunities Wording Microtest Review

Scorer ID: choosing-micro-scorer-001

Skill SHA-256: `9c0a4b40025c646bbb494d4159fbf8b4b3e2861a6361f498544f435baacfa49a`

I independently read and scored all ten frozen responses. Two control repetitions had initially been written to a stray top-level directory; I moved those unchanged response bytes and their manifests into the canonical control directory before verification. The canonical artifact now contains the exact five-control and five-guided response/manifest sets. Normalizing heterogeneous field names, the manifests have ten unique generator tasks and ten unique response hashes. Every prompt matches the rubric exactly, every control skill hash is null, every guided skill hash matches the current frozen skill, and every declared byte hash matches. All JSON parses. Responses have no trailing horizontal whitespace and valid boundary whitespace.

Scoring required every clause of a criterion to be operationalized. All controls choose C using direct evidence, reject fashion and clicks, and order opportunity choice before user learning. Only control repetition 1 states a concrete specialist-access and safety prerequisite for reopening radiology; the other controls notice harm and absent access but do not define a reconsideration gate. Controls 2–5 additionally hold broad acquisition until a paid pilot, repeated value, or repeated use. No control returns the complete per-candidate opportunity record or the four-field learning handoff. Control scores are `[3, 2, 2, 2, 2]`.

All guided repetitions correctly choose C, gate radiology, order opportunity choice before learning, delay primary growth ownership, and hand learning an explicit hypothesis, sample, evidence threshold, and review date. The honest systematic miss is criterion 2 in guided repetitions 1–4: they give all three candidates dispositions and reopening conditions, but develop the complete opportunity record only for C. Guided repetition 5 explicitly requires a complete record for every option and passes all criteria. Guided scores are `[4, 4, 4, 4, 5]`.

Verdict: the frozen guidance materially improves the strict normalized score from `0.44` to `0.84` (`+0.40`) and raises the all-criteria pass rate from `0.00` to `0.20`. It does not justify a claim that the opportunity-record contract lands consistently across repetitions.
