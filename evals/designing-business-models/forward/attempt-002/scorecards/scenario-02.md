# scenario-02 — Score: 4/5

1. **Pass.** The response rejects “core” as ownership evidence and explicitly compares delay, rework, dependency, switching, fixed capital, management load, missing capability, and scale-sensitive landed cost.
2. **Pass.** It uses the validated-hardware context, six-week qualification, reliability and certification gates, inventory and working-capital effects, and the twelve-month internal-line latency while treating internal cost and ramp as facts to verify.
3. **Pass.** It selects a bounded second-source qualification plus standard interface before internal capex and requires instrumentation that distinguishes supplier, interface, installation, and system failures.
4. **Pass.** It sets day-45 and day-90 reviews, explicit quality and lead-time thresholds, and named `integrate`, `dual-source`, `partner`, `repair`, and `stop` branches tied to reliability, delay, replaceability, dependency, capital, and capability.
5. **Miss.** It assigns the boundary to business-model design, the interface to `engineering-for-leverage`, and financing to `raising-and-governing-capital`, but it never assigns customer-visible pilot or rollout exposure to `shipping-and-iterating-products`.

## Exact misses

- Criterion 5: The required `shipping-and-iterating-products` owner for user-visible rollout is absent.
