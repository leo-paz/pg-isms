# Fresh-context heldout review

Case: `focus-heldout-buyer-001`  
Scorer: `/root/focus_heldout_scorer`  
Vector: `[1, 0, 0, 0, 0]`  
Score: `1/5`  
Verdict: **FAIL**

## Review

The response gets the central precedence decision right. It plainly identifies the live acquisition/exclusivity process as primary, limits the focus-and-morale material to supporting cadence, and frames July 22 as an exclusivity decision rather than a sale decision. That is sufficient for C1.

C2 is close but fails under the all-components rule. The observed/claimed split is explicit, the major economics and standalone case have dated owners and tests, the customer-impact test is concrete, and the headline price is correctly treated as non-binding rather than proceeds. Yet the response never explicitly identifies close probability as an unknown, and it does not make diligence burden a distinct unknown with the full owner/method/artifact/date/threshold/decision-effect chain. Testing buyer seriousness, diligence scope, backlog, and hours is useful but not an explicit completion of those required elements.

C3 also fails narrowly but unambiguously. The response bounds the one buyer decision, disclosure, data-room scope, management session, and alternative calls; it freezes the rebrand, cancels or delegates the keynote, and defers the offsite. It never explicitly disposes of optional roadmap work or generic calendar redesign. The global work-classification rule is not enough when the criterion requires every listed deferral to be explicit.

C4 contains strong humane safeguards: no work after 9 p.m., 11 hours of recovery, no compulsory weekends, preemption of diligence by incidents and customer risk, and a sharp cap on the product lead's deal work. It nevertheless omits a sustainable weekly limit. It also names backups only for the CEO and CFO, not backup coverage for migration, renewals, support/on-call/incidents, legal/fiduciary duties, and secure data handling. Those omissions zero the bundle.

C5 has useful dated checks, observable thresholds, a counter capped at 14 days, a standalone option, and a July 23 handoff checkpoint. It does not explicitly supply a pause branch or a next owner for every proceed, counter/extend, pause, and decline/end branch. It also does not explicitly keep acquisition work primary after proceeding or map pause, end, and eventual close to dated handoffs back to `operating-with-focus-and-morale`. The generic binary handoff to a transaction cadence or ordinary focus is too compressed for the strict condition.

## Integrity and leakage assessment

Integrity passes. The independently computed response hash, byte count, word count, birth time, and modification time all match the generation attestation. The independently computed case, generation-plan, and skill-freeze hashes match their frozen references. The protocol attests fresh generation context, hidden criteria, one-write generation, no post-creation modification, and a generator distinct from this scorer. The actual response satisfies the required headers and word maximum.

Naturalness/no-leakage passes. The response is specific to the company, dates, owners, buyer request, customer work, and workload. It does not expose criterion identifiers, scoring mechanics, thresholds for passing the evaluation, hidden-answer text, or essay attribution. Its skill names and structured operating labels are compatible with the visible skill/runtime context authorized for generation.

The transfer rule requires C1 plus at least four total positive bits. C1 passes, but the total is only one; transfer therefore fails.
