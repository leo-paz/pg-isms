# Held-out evaluation review

- Schema version: 1
- Case ID: `positioning-heldout-018`
- Attempt: 18
- Scorer task: `/codex-exec/positioning-heldout-scorer-018`
- Scorer session: `019f6417-d47b-7961-b97e-899a9f307c74`

## Overall judgment

Integrity is **PASS**. The response scores **4/5** with vector `[true, true, true, false, true]`. Because the threshold is 4/5 plus integrity PASS, and because the prompt is genuinely natural rather than recipe-shaped, transfer is **true** and the verdict is **PASS**.

The response is unusually complete on evidence discipline. It keeps the validated buyer-owned core distinct from the shared-review hypothesis, derives the alternative set, makes resource opportunity cost explicit, gives locally executable tests, and converts the pilot into a dated board decision. It also treats the historical rollback as one observed recovery rather than proof of scaled safety.

The single scoring failure is consequential, not stylistic. The outcome section announces ordered precedence, places `stop-platform-bet` first, and lets that branch fire whenever recurring complement value is absent. That formulation consumes the very state that the later `keep-integrated` branch is meant to handle: a validated core-owned need with absent independent complement value. The earlier recommendation contains the correct exception, but a conjunctive criterion requiring exact runtime precedence cannot be satisfied by a correct sentence elsewhere when the executable branch definitions contradict it.

## Prompt naturalness and recipe leakage

The prompt is a natural executive/product scenario. Its detail is domain evidence: the manual review workflow, buyer usage and renewals, the bounded pilot, open causal questions, measurement latency, fixed staffing, opportunity cost, safety constraints, and one rollback drill. Those details are appropriate inputs to the decision and do not themselves teach the evaluator's method.

There is no material recipe leakage. The prompt does not provide:

- a complete manual/integrated/interface/specialist/ecosystem/same-resource comparison taxonomy;
- the canonical advantage chain from mechanism through continuing refresh;
- the full nine-field local test contract plus cutoff rationale/tradeoff/revision requirements;
- the exact four allowed outcomes, their precedence, or the runtime-ordered skill handoffs; or
- a generic rollback-record schema.

Supplying the current workflow, the current pilot, the resource cap, and same-resource cost does not leak the missing integrated/interface/specialist/ecosystem option map. The generator still had to derive that structure. Prompt naturalness therefore passes independently of the stored boolean.

## Exact failure evidence

Criterion 4 fails on response lines 129, 133, and 137:

- Line 129: "Apply precedence in the order below."
- Line 133: `stop-platform-bet` applies when "there is no completed builder-user loop or recurring complement value by September 18."
- Line 137: `keep-integrated` applies when a validated buyer-owned need exists while suppliers and auditors show no independent complement value.

Under the declared ordering, absent recurring complement value satisfies the first branch even when the buyer-owned need is validated. The later integrated branch is therefore shadowed. The required ordering instead needs hard breach first, then the validated-core/no-complement exception, then other absent-value or absent-loop states, followed by bounded-interface and expansion gates.

No other criterion failure is recorded. Criterion 2 is not failed for marking copy barrier and refresh unknown, because the criterion expressly permits evidence breaks. Criterion 3 is not expanded to uncertainties that do not control a branch. Criterion 5 is not inflated with verbatim rollback timestamps or an extra record schema.
