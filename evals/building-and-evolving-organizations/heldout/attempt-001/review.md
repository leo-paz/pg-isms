# Preserved Held-out Review — Attempt 001

Attempt 001 is a valid, integrity-clean transfer failure and remains unchanged.
The skill was frozen before authorship. A distinct author, prompt reviewer,
generator, and scorer handled the case. The prompt gate first rejected two hidden
rubric defects; criteria-only remediation preserved the prompt, then passed the
gate before response generation.

The frozen response scored `[0, 0, 1, 1, 1]`, or `3/5`, below the predeclared
`4/5` threshold. It passed the decision-rights/continuity design, bounded test, and
closure criteria. It failed C1 because no explicit finance-test result switched
`managing-runway-and-survival` to primary, and failed C2 because forecast
robustness and whether survival binds were not explicit `unknown` ledger rows.
Those misses are not inferred away or repaired after generation.

Integrity, chronology, response hash, whitespace, prompt naturalness, and
no-leakage checks all pass. A genuinely new attempt with distinct identities is
required for a passing held-out transfer result.
