# Preserved Held-out Review — Attempt 002

Attempt 002 is a distinct, integrity-clean role-evolution transfer failure. Its
author, prompt reviewer, generator, and scorer differ from attempt 001, and the
generator saw only a prompt projection plus the frozen package.

The response scored `[1, 0, 0, 0, 0]`, or `1/5`, below the predeclared `4/5`
threshold. Routing passed. Strict misses were: incomplete candidate/role-fit
unknown timing; no increased recruiting allocation; no forward recruiting-latency
measure; and incomplete explicit bias/proxy safeguards for a later search. Those
requirements were fixed before generation and are not inferred from adjacent
strong content.

Integrity, chronology, prompt naturalness, prompt-only isolation, response hash,
and whitespace all pass. The response and rubric remain immutable. A new case with
new identities is required for the passing transfer gate.
