# Forward attempt 4 failure

Attempt 4 is invalid under its frozen package limit and was not scored. Four first completions are within 1,050 words; scenario 5 contains 1,096 words, 46 above the limit. Scenario 3 is below the target band, but the target was not a minimum validity gate.

All five response bodies remain byte-for-byte frozen. Their hashes, byte counts, and equal birth/modify timestamps are recorded in `failure-attestation.json`. No criteria were visible to generators and scoring did not start.

The next attempt requires a material package-level refactor addressing the remaining competing-offer disclosure/comparison pressure, new package hashes, and five new fresh-context generators. This failure does not permit response replacement or resampling.
