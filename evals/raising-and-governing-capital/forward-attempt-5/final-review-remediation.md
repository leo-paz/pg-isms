# Attempt 5 final-review remediation

A fresh behavior review found circular offer predicates after scoring and adjudication. `Za` required company-signed acceptance before the branch action signed the acceptance, and `Zc` required completed funding before the branch action closed. The review also found that section word budgets were written as hard independent limits even though several otherwise valid outputs exceeded them.

The package was changed only after attempt 5 was fully frozen. The next package separates acceptance readiness (`ZaR`) from signed-acceptance evidence (`ZaE`) and closing readiness (`ZcR`) from effective-document and cleared-funds evidence (`ZcE`). It also makes section budgets soft compaction targets, defines prompt-supplied historical measurements as observed-at-input, and narrows the business-model route-away to unsettled capital-model or return-shape questions.

Attempt 5 response, scoring, audit, adjudication, and body-wrapper bytes remain preserved. A fresh complete attempt is required before commit.
